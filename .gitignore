runpod/r2.env
runpod/downloads/
artifacts/
logs/
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
runpod/pod.env
.fuse_hidden*
*.pt
*.pth
uv.lock
*.egg-info/
.claude/scheduled_tasks.lock

# Derived study outputs. Results must be reproducible from a commit, not
# carried in the tree; see `make paper`.
# requirements.lock.txt is deliberately NOT ignored: it is the record of which
# environment produced the results, and is useless if it is not committed.
artifacts/
